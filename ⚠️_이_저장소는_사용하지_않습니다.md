# ⚠️ 이 저장소는 더 이상 사용하지 않습니다

Render(실제 서버)가 배포하는 저장소는 **`my-safety-app`** 입니다.
이 저장소에 코드를 올려도 서버에 반영되지 않습니다.

## 어디에 무엇을 올려야 하나

| 올릴 내용 | 저장소 | 배포처 |
|---|---|---|
| 서버(API) | **my-safety-app** | Render |
| 관제센터 | safety-meerkat-admin | GitHub Pages |
| 근로자 앱(PWA) | safety-meerkat-app | GitHub Pages |
| 웹 플랫폼 | safety-meerkat-web | GitHub Pages |

## 왜 이렇게 됐나

Render 서비스가 처음 만들어질 때 `my-safety-app` 저장소에 연결됐는데,
이후 서버 코드를 이 저장소(`safety-meerkat-server`)에 올리면서 둘이 갈라졌습니다.
2026-09-10에 `my-safety-app`을 서버 저장소로 정리했습니다.

이 저장소를 정리하고 싶으시면 GitHub Settings에서 Archive 처리하시면 됩니다
(삭제하지 않아도 이 안내만으로 혼동은 막을 수 있습니다).
