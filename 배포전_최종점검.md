# 배포 준비 점검 (2026-08-28, 최종 갱신 2026-08-30)

이 문서는 지금까지의 모든 후속조치가 실제로 배포 가능한 상태인지 확인한 결과와, 실제 배포 시 따라야 할 순서를 정리한 것입니다. Docker 자체(`docker build`)는 이 작업 환경에 Docker가 없어 직접 실행해보지 못했지만, **Dockerfile이 실행하는 모든 로직(환경변수 검증, 비루트 실행, graceful shutdown, 헬스체크 응답)은 실제로 그 조건 그대로 재현해서 검증했습니다.**

## 📦 최종 산출물 3종

| 파일 | 배포 대상 | 비고 |
|---|---|---|
| `안전미어캣프로_PWA.zip` | 정적 웹호스팅 (Netlify/Vercel/GitHub Pages 등) | `index.html`+`manifest.json`+`sw.js`+아이콘 — 웹 버전과 설치형 앱(PWA) 버전이 동일한 파일입니다 |
| `safety-meerkat-server.zip` | Node.js 실행 가능한 PaaS (Railway/Fly.io 등) | 백엔드 API 서버 |
| `admin-dashboard.zip` | 정적 웹호스팅 | 관제센터(웹 관리자 화면) |

## ✅ 이번에 직접 실행해서 확인한 것

| 항목 | 확인 방법 | 결과 |
|---|---|---|
| `NODE_ENV=production` + `JWT_SECRET` 없음 → 기동 거부 | 실제로 그 조합으로 실행 | ✅ 거부됨 |
| `NODE_ENV=production` + `CORS_ORIGIN` 없음 → 기동 거부 | 실제로 그 조합으로 실행 | ✅ 명확한 오류 메시지와 함께 거부됨 |
| `NODE_ENV=production` + 필수값 모두 설정 → 정상 기동 | 실제로 그 조합으로 실행 + 스모크테스트 10건 | ✅ 전부 통과 |
| CORS: 허용된 오리진만 `Access-Control-Allow-Origin` 응답 | curl로 허용/비허용 오리진 각각 요청 | ✅ 허용된 오리진만 헤더 응답, 그 외엔 헤더 자체가 없음 |
| SIGTERM 수신 시 정상 종료(graceful shutdown) | 실제 프로세스에 `kill -TERM` 전송 | ✅ "SIGTERM 수신 → 정상 종료 완료" 로그 확인, DB 핸들도 정리됨 |
| `/health` 엔드포인트가 rate limit보다 먼저 응답 | 코드 검토 중 발견 — 원래는 rate limit 뒤에 있었음 | ✅ 앞으로 이동해서 수정 (로드밸런서+Docker+외부 모니터링이 겹쳐도 헬스체크가 절대 막히지 않도록) |
| `.env.example`이 코드가 실제로 읽는 환경변수와 일치 | `grep`으로 코드의 `process.env.*` 전수 추출 후 대조 | ✅ 5개(JWT_SECRET, NODE_ENV, PORT, CORS_ORIGIN, DB_PATH) 전부 일치 |

## 🔧 점검 중 발견해서 함께 고친 것

- **`.dockerignore`에 `uploads/`가 빠져있었습니다** — 로컬에서 테스트하며 쌓인 첨부파일이 Docker 이미지에 그대로 포함될 뻔했습니다. 추가했습니다.
- **Dockerfile·fly.toml.example 주석이 "README 8절"을 참고하라고 돼 있었는데, 실제 배포 안내는 3절("배포 준비")입니다.** 8절은 마이그레이션 섹션입니다. 두 파일 모두 3절로 고쳤습니다.
- **`CORS_ORIGIN` 관련 `.env.example` 주석이 예전 동작(느슨한 경고) 기준으로 쓰여 있었습니다.** 실제로는 `JWT_SECRET`과 동일하게 운영에서 미설정 시 기동 자체를 거부하도록 강화됐는데 주석이 그걸 반영 못 하고 있어서 갱신했습니다.
- **`/health`가 rate limiter 뒤에 있었습니다.** 지금 수치(분당 300건)로는 실질적 문제가 없지만, 헬스체크는 인프라의 생명줄이라 아예 rate limit 적용 범위 밖으로 옮겼습니다.

## 📋 실제 배포 순서 (Railway 기준 — Fly.io는 `fly.toml.example` 참고)

1. **저장소 준비**: `safety-meerkat-server.zip`을 풀어서 Git 저장소로 만들거나 기존 저장소에 반영합니다.
2. **영구 볼륨 생성**: Railway 대시보드에서 이 서비스에 Volume을 추가하고, 마운트 경로를 예: `/app/data`로 지정합니다. **이걸 빼먹으면 재배포할 때마다 DB와 첨부파일이 전부 사라집니다.**
3. **환경변수 설정** (Railway 대시보드 → Variables):
   - `NODE_ENV=production`
   - `JWT_SECRET=`(터미널에서 `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`로 생성한 값)
   - `CORS_ORIGIN=`(프론트엔드를 서빙할 실제 도메인. 예: `https://safety-meerkat.netlify.app`. 여러 개면 콤마로 구분)
   - `DB_PATH=/app/data/data.sqlite` (2번의 볼륨 마운트 경로와 반드시 일치해야 함)
4. **배포**: Railway가 Dockerfile을 인식해 자동으로 빌드·배포합니다.
5. **배포 직후 확인**: `curl https://<배포된주소>/health` → `{"ok":true,...}` 확인
6. **첫 관리자 계정 생성**: 브라우저로 `https://<배포된주소>/setup`에 접속하면 회사명·관리자 아이디·비밀번호를 입력하는 화면이 뜹니다. 입력하고 제출하면 끝입니다(터미널/curl 불필요). 이 화면은 회사가 하나도 없을 때만 나타나고, 이후 접속하면 자동으로 "이미 시작된 서버입니다" 안내로 바뀝니다.
7. **프론트엔드 연결**: `안전미어캣프로_PWA.zip`을 정적 호스팅에 올린 뒤, 그 주소로 접속해 설정 화면에서 서버 주소를 입력하고 방금 만든 관리자 계정으로 로그인 → 동의 게이트 완료. 모바일에서 "홈 화면에 추가"로 설치하면 앱처럼 쓸 수 있습니다.
8. **관제센터 연결**: `admin-dashboard.zip`을 정적 호스팅(Netlify, Vercel, 또는 같은 서버의 정적 파일로)에 올리고 같은 서버 주소로 로그인.

## 🔄 업데이트 배포 시 (2026-09-05 추가 — 중요)

**이미 운영 중인 서버에 새 버전을 올릴 때**는 기존 데이터가 그대로 살아있어야 합니다.
이 부분을 실제로 시뮬레이션해서 확인했고, 그 과정에서 서버가 아예 기동하지 못하는
문제를 발견해 고쳤습니다(테이블을 만들기 전에 컬럼을 추가하려 해서 발생).

### 배포 순서 (권장)
1. **먼저 백업을 받으세요.** `curl -H "X-Backup-Key: 그 값" https://<주소>/admin/backup -o backup.sqlite`
   — 무슨 일이 생겨도 되돌릴 수 있는 유일한 보험입니다.
2. GitHub에 새 코드를 올리면 Render가 자동으로 재배포합니다.
3. 재배포 후 `https://<주소>/health`를 확인하세요. `{"ok":true}`가 나오면 정상입니다.
4. 앱에서 기존 데이터(아차사고·위험성평가 등)가 그대로 보이는지 한 번 확인해주세요.

### 만약 기동에 실패하면
Render 로그에서 `no such table` 또는 `no such column`이 보이면 마이그레이션 문제입니다.
이 경우 **당황해서 DB를 지우지 마시고** 1번에서 받아둔 백업을 보관한 채로 알려주세요.

### 검증 방법
`verification-scripts/server-tests/migration-audit.js`가 이 시나리오를 자동으로 확인합니다.
구버전 DB에 실제 데이터를 넣고 새 서버를 띄운 뒤, 데이터가 살아남는지·신규 테이블이
생기는지를 검사합니다. 새 기능을 추가할 때마다 이 테스트를 돌리면 같은 사고를 막을 수 있습니다.

## 🗄️ 백업 (2026-08-30 신규 — 출시단계 필수 항목)

**여러 회사의 실제 데이터가 쌓이기 시작한 지금, 백업 없이 운영하는 것은 가장 위험한 상태입니다.** 이번에 두 겹으로 방어선을 만들었습니다.

1. **자동 스냅샷**: 서버가 매일 자동으로 DB 스냅샷을 뜨고, 7일 지난 것은 자동 삭제합니다. 같은 서버 디스크 안에 저장되므로 "실수로 데이터를 지웠다/마이그레이션이 잘못됐다/버그로 데이터가 깨졌다" 같은 사고는 이걸로 되돌릴 수 있습니다. 다만 **디스크 자체가 통째로 고장 나면 이것도 같이 사라집니다** — 그래서 2번이 반드시 필요합니다.

2. **수동 다운로드**: `GET /admin/backup` — 실제 SQLite 파일을 그대로 다운로드받습니다. **정기적으로(주 1회 정도) 직접 눌러서 본인 컴퓨터나 Google Drive 등 서버와 완전히 분리된 곳에 보관해주세요.** 이게 디스크 자체가 죽어도 살아남는 유일한 방법입니다.

### 사용 방법
1. Render 환경변수에 `BACKUP_SECRET` 하나를 추가하세요 (JWT_SECRET처럼 아무도 모르는 긴 무작위 문자열 — `.env.example`에 생성 명령어가 있습니다). **이 값은 절대 다른 사람과 공유하지 마세요** — 이 값을 아는 사람은 이 서버를 쓰는 모든 회사의 데이터를 통째로 받을 수 있습니다.
2. 백업받을 때:
   ```bash
   curl -H "X-Backup-Key: 여기에 BACKUP_SECRET 값" https://<서버주소>/admin/backup -o backup.sqlite
   ```
3. `BACKUP_SECRET`을 설정하지 않으면 이 기능 자체가 꺼집니다(404) — 안전한 쪽으로 기본값이 잡혀있습니다.

## 🔭 운영 모니터링 (2026-08-30 신규 — 출시단계 필수 항목)

**지금까지는 서버에 오류가 나도, 사용자가 신고하기 전까진 아무도 몰랐습니다.** 두 가지를 준비했습니다.

### 1. 서버가 죽었는지 — 무료 외부 감시 (직접 설정 필요, 5분)
1. [uptimerobot.com](https://uptimerobot.com) 무료 가입 (카드 필요 없음)
2. **Add New Monitor** → Monitor Type: `HTTP(s)` → URL에 `https://<서버주소>/health` 입력
3. Monitoring Interval: 5분 (무료 요금제 기본값)
4. 알림 받을 이메일 등록
→ 서버가 5분 이상 응답 없으면 이메일로 바로 알려줍니다. 무료 요금제로 충분합니다.

### 2. 서버는 살아있는데 오류가 나고 있는지 — `GET /admin/errors`
백업과 같은 `BACKUP_SECRET`으로 보호됩니다(추가 설정 불필요).
```bash
# 최근 오류 50건 확인
curl -H "X-Backup-Key: 그 값" https://<서버주소>/admin/errors

# 확인 다 했으면 비우기
curl -X DELETE -H "X-Backup-Key: 그 값" https://<서버주소>/admin/errors
```
요청 본문이나 비밀번호 같은 민감정보는 절대 여기 안 남습니다 — 오류 메시지·발생 위치·시각만 기록됩니다. Express 라우트 안의 오류든, 그 바깥(비동기 처리 중 예외)에서 난 오류든 전부 여기 모입니다.

## ⚠️ 배포 전 최종 확인이 필요한 것 (이 문서만으로는 해결 안 됨)


- **개인정보처리방침 문서**는 2026-08-30에 보편적으로 쓰이는 표준 양식으로 다시 작성했습니다(대괄호 자리표시자 제거, 자연스러운 문장으로 교체). 사업자등록번호는 임시로 "000"을 넣어뒀습니다 — 사업자 등록이 완료되면 실제 번호로 교체하고, `src/routes/auth.js`의 `CURRENT_POLICY_VERSION`과 프론트엔드 2곳(`안전미어캣프로_PWA`, `admin-dashboard`)의 `POLICY_VERSION`을 함께 올려서 기존 이용자도 재동의하게 하세요.
- Railway/Fly 등 실제 호스팅 리전이 어디인지에 따라 개인정보 국외이전 고지가 필요할 수 있습니다.
- 이 문서에 적힌 배포 순서 자체는 실제 Railway 계정으로 끝까지 실행해본 것은 아닙니다(이 작업 환경엔 그럴 수단이 없습니다) — Dockerfile이 하는 일 하나하나를 로컬에서 동일한 조건으로 재현해서 검증한 것입니다. 실제 배포 시 플랫폼 특유의 이슈(빌드 타임아웃, 볼륨 마운트 타이밍 등)가 있을 수 있으니 첫 배포 후 반드시 위 5~6번 단계로 직접 확인하세요.
